services:
  bitdoge-hourly-miner:
    image: ghcr.io/foundry-rs/foundry:stable
    container_name: bitdoge-hourly-miner
    restart: unless-stopped
    environment:
      PRIVATE_KEY: ${PRIVATE_KEY}
    volumes:
      - ./docker/miner.sh:/miner.sh:ro
    entrypoint: ["/bin/sh", "/miner.sh"]
